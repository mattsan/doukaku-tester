require 'spec_helper'

describe Doukaku::Tester do
  it 'has a version number' do
    expect(Doukaku::Tester::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
